# .github

TopPrism profile and repository-governance materials.

The current GitHub account is a personal `User` account, so the public profile
is rendered from the root `README.md` in `topprismdata/topprismdata`. The
`profile/README.md` path is retained as an Organization-profile source only;
it must not become a second, drifting brand narrative.

See [`README_CONTRACT.md`](README_CONTRACT.md) for the shared repository README
contract.
